https://developers.binance.com/docs/zh-CN/binance-spot-api-docs/README

```
from winsdk.windows.ui.notifications.management import UserNotificationListener

from winsdk.windows.ui.notifications import KnownNotificationBindings

  
  

verification_number = None

  
  

def handler(a, b):

    global verification_number

    unotification = a.get_notification(b.user_notification_id)

    # print(dir(unotification))

    if hasattr(unotification, "app_info"):

        # print("App Name: ", unotification.app_info.display_info.display_name)

        text_sequence = unotification.notification.visual.get_binding(KnownNotificationBindings.get_toast_generic()).get_text_elements()

        it = iter(text_sequence)

        # print("Notification title: ", it.current.text)

        while True:

            next(it, None)

            if it.has_current:

                if "varification" in it.current.text:

                    verification_number = it.current.text[-6:-1]

                    print(verification_number)

            else:

                break            

    else:

        pass

  

listener = UserNotificationListener.current

listener.add_notification_changed(handler)


```




```
import asyncio

import winsdk.windows.ui.notifications.management as management

  

def handler(a, b):

    print("hello!!!")

  

async def main():

    listener = management.UserNotificationListener.current

    access_result = await listener.request_access_async()

    print(access_result) #prints 2: denied

  

    # listener.add_notification_changed(handler)

  
  

if __name__ == "__main__":

    asyncio.run(main())

```